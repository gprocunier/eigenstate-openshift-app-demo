#!/usr/bin/ansible-playbook
---
- name: Validate podinfo HTTPS route and vault proof
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - ../vars/podinfo.yml

  tasks:
    - name: Check route object
      ansible.builtin.command:
        argv:
          - oc
          - -n
          - "{{ demo_namespace }}"
          - get
          - route
          - "{{ route_name }}"
      register: route_state
      changed_when: false

    - name: Validate /version endpoint over HTTPS
      ansible.builtin.uri:
        url: "https://{{ route_fqdn }}/version"
        method: GET
        return_content: true
        validate_certs: false
        status_code: 200
      register: version_response
      changed_when: false

    - name: Validate /metrics endpoint over HTTPS
      ansible.builtin.uri:
        url: "https://{{ route_fqdn }}/metrics"
        method: GET
        return_content: true
        validate_certs: false
        status_code: 200
      register: metrics_response
      changed_when: false

    - name: Retrieve archived TLS bundle proof from IdM vault
      ansible.builtin.set_fact:
        vault_record: >-
          {{ lookup('eigenstate.ipa.vault',
                    vault_name,
                    shared=vault_shared,
                    server=ipa_server,
                    ipaadmin_principal=ipaadmin_principal,
                    ipaadmin_password=lookup('env', 'IPA_ADMIN_PASSWORD'),
                    result_format='record',
                    include_metadata=true,
                    verify=ipa_verify) }}
      no_log: true

    - name: Show validation proof
      ansible.builtin.debug:
        msg:
          - "{{ route_state.stdout }}"
          - "version: {{ version_response.content }}"
          - "metrics sample: {{ (metrics_response.content.splitlines() | list)[:8] }}"
          - "vault: {{ vault_name }} retrieved; payload hidden"
